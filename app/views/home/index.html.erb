<h2>My APP</h2>

<% if user_signed_in? %>
    <%= "Bienvenido #{current_user.name}!" %>
    <%= image_tag(current_user.image.thumb('200×200').url ) if current_user.image_stored? %>
    <%= link_to 'Cerrar sesion' , destroy_user_session_path, method: :delete %>
<% end %>